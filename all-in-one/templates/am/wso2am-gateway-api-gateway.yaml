# -------------------------------------------------------------------------------------
#
# Copyright (c) 2023, WSO2 LLC. (http://www.wso2.com). All Rights Reserved.
#
# This software is the property of WSO2 LLC. and its suppliers, if any.
# Dissemination of any information or reproduction of any material contained 
# herein is strictly forbidden, unless permitted by WSO2 in accordance with the 
# WSO2 Commercial License available at https://wso2.com/licenses/eula/3.2
#
# --------------------------------------------------------------------------------------

{{- if .Values.kubernetes.gatewayAPI.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ template "am-all-in-one.fullname" . }}-gateway
  namespace: {{ .Release.Namespace }}
{{- if .Values.kubernetes.gatewayAPI.annotations }}
  annotations:
{{ toYaml .Values.kubernetes.gatewayAPI.annotations | indent 4 }}
{{- end }}
{{- if .Values.kubernetes.gatewayAPI.labels }}
  labels:
{{ toYaml .Values.kubernetes.gatewayAPI.labels | indent 4 }}
{{- end }}
spec:
  gatewayClassName: {{ .Values.kubernetes.gatewayAPI.gatewayClassName }}
  listeners:
  {{- if .Values.kubernetes.gatewayAPI.management.enabled }}
  - name: management-https
    hostname: {{ .Values.kubernetes.gatewayAPI.management.hostname }}
    port: 8443
    protocol: HTTPS
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: {{ .Values.kubernetes.gatewayAPI.tlsSecret }}
    allowedRoutes:
      namespaces:
        from: Same
  {{- end }}
  {{- if .Values.kubernetes.gatewayAPI.gateway.enabled }}
  - name: gateway-https
    hostname: {{ .Values.kubernetes.gatewayAPI.gateway.hostname }}
    port: 8443
    protocol: HTTPS
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: {{ .Values.kubernetes.gatewayAPI.tlsSecret }}
    allowedRoutes:
      namespaces:
        from: Same
  {{- end }}
  {{- if .Values.kubernetes.gatewayAPI.websocket.enabled }}
  - name: websocket-https
    hostname: {{ .Values.kubernetes.gatewayAPI.websocket.hostname }}
    port: 8443
    protocol: HTTPS
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: {{ .Values.kubernetes.gatewayAPI.tlsSecret }}
    allowedRoutes:
      namespaces:
        from: Same
  {{- end }}
  {{- if .Values.kubernetes.gatewayAPI.websub.enabled }}
  - name: websub-https
    hostname: {{ .Values.kubernetes.gatewayAPI.websub.hostname }}
    port: 8443
    protocol: HTTPS
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: {{ .Values.kubernetes.gatewayAPI.tlsSecret }}
    allowedRoutes:
      namespaces:
        from: Same
  {{- end }}
{{- end }}
